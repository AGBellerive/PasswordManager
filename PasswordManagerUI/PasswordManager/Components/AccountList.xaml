<UserControl x:Class="PasswordManager.Components.AccountList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PasswordManager.Components"
             mc:Ignorable="d" 
             x:Name="root"
             d:DesignHeight="500" d:DesignWidth="800">
    <Grid>
        <ScrollViewer x:Name="AccountListScroller" Style="{StaticResource ScrollViewerTheme}" Visibility="Visible">
            <ListBox x:Name="AccountListBox" BorderThickness="0" Background="Transparent">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text ="{Binding Site}" Width="230" Padding="2px" Cursor="Hand" Foreground="White" TextDecorations="Underline" FontWeight="Bold" FontFamily="{StaticResource UniversalFont}" FontSize="16" MouseLeftButtonDown="OnAccountClick"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </ScrollViewer>
    </Grid>
</UserControl>
